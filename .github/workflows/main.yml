name: Download Kirby Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Download image
        run: |
          curl -L --http1.1 --max-time 60 -o 2760.jpg https://codecraftsupport.com/Kirby/DATA/Images/2760.jpg
          ls -lh 2760.jpg
