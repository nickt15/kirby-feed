name: Download Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Download image
        run: |
          curl -L -o 2760.jpg https://codecraftsupport.com/Kirby/DATA/Images/2760.jpg
          ls -lh 2760.jpg
